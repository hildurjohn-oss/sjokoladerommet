{
  "permissions": {
    "allow": [
      "PowerShell($env:PATH = \"$env:LOCALAPPDATA\\\\Programs\\\\Git\\\\cmd;\" + $env:LOCALAPPDATA + \"\\\\Programs\\\\Git\\\\bin;\" + $env:PATH; git init; git add .; git commit -m \"Initial commit: Sjokoladerommet website\")"
    ]
  }
}
